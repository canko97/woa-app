FROM node:21.2-alpine3.18

ARG SMTP_AUTH_USER
ARG SMTP_AUTH_PASS

WORKDIR /app

COPY package.json ./

RUN npm install

COPY ./ ./

ENV SMTP_AUTH_USER=$SMTP_AUTH_USER
ENV SMTP_AUTH_PASS=$SMTP_AUTH_PASS

CMD ["npm", "start"]

